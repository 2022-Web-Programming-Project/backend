<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if (isWatch) { %>
        <link href="/styles/watch.css" rel="stylesheet" type="text/css">
    <% } %>
    <% if (!isWatch) { %>
        <link href="/styles/index.css" rel="stylesheet" type="text/css">
    <% } %>
    <script src="/scripts/theme.js"></script>
    <title>Index</title>
</head>
<body>
    <div id="wrapper">
        <div class="back">
            <div class="index">
                <% if (user) { %>
                    <h1>
                        <%= user?.username %>님 반갑습니다.
                        <p><a href="/auth/logout">logout</a></p>
                    </h1>
                <% } %>
    
                <% if (!user) { %>
                    <h1>먼저 로그인해주세요!</h1>
    
                    <p><a href="/auth/login">login</a>
                        <a href="/auth/regist">regist</a></p>
                <% } %>
    
                <% if (isWatch) { %>
                    <p>워치를 사용중이시군요!</p>
                <% } %>
                
                <p><a href="/lunch">오늘의 점심</a></p>
                <p><a href="/timetable">시간표</a></p>
    
                <br>
                <br>
                <button id="togTheme">Toggle dark mode</button>
            </div>
        </div>
    </div>
</body>
</html>